<h1>Hi there <%= current_user.email if current_user %>.</h1>

<% if !current_user %>
  <%= link_to ("Sign in with Facebook", user_facebook_omniauth_authorize_path) %>
<% else %>
  <%= link_to 'Sign out', with destroy_user_session_path, method: 'DELETE' %>
<% end %>